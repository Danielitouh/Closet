# Project-local mcporter config written by `agent-reach install` at runtime.
# The SessionStart hook mirrors it to ~/.mcporter so it isn't needed in git.
/config/

# Wiki app
/node_modules/
app/node_modules/
app/dist/
app/smoke.tmp.mjs
