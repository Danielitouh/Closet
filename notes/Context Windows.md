---
tags: [ai]
---
# Context Windows

The working memory budget of an LLM. A second brain is the antidote to context limits: persist knowledge as [[Atomic Notes]] and retrieve only what the task needs — the pattern behind [[RAG]].
